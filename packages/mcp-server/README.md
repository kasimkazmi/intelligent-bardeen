# superpowers-mcp-server

This package wraps the `@superpowers/core` state engine into a Model Context Protocol (MCP) server, exposing tools to start sessions, check items off lists, and run verifications.
